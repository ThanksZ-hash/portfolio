# 개발 과정

## 1. Gemini API 학습 및 테스트
- Gemini API(Python, `google-genai`) 기본 사용법 확인
- `.env`에 `GEMINI_API_KEY` 저장, `python-dotenv`로 로드
- `gemini_test.py`로 텍스트 입력 → 텍스트 출력 테스트

## 2. 번역기 구조 설계
- 처음엔 정적 `trans.html`에서 브라우저가 Gemini REST API를 직접 호출하는 구조로 시작
- **문제**: 브라우저에 API 키가 그대로 노출됨
- **개선**: FastAPI + 정적 HTML 구조로 전환
  - `app.py`: `/` 에서 `trans.html` 서빙, `/api/translate`에서 서버가 Gemini API 호출
  - API 키는 서버(`.env`)에서만 사용, 클라이언트에는 노출되지 않음

## 3. 기본 기능 구현
- 번역 방향 선택(한국어 → English / English → 한국어)
- 방향 전환(⇄) 버튼
- 1000자 제한: 초과 시 유료 전환 안내 표시 (프론트 + 백엔드 이중 체크)

## 4. 번역 정확도 버그 수정
- **버그**: 입력 텍스트가 명령어/질문처럼 보이면 Gemini가 번역 대신 마음대로 대답하거나 요약함 (예: 반복된 셸 명령어 입력 시 "안녕하세요..."로 시작하는 엉뚱한 응답)
- **원인**: 모델이 입력 내용을 "번역할 데이터"가 아니라 "응답해야 할 요청"으로 오해
- **해결**:
  - `system_instruction`으로 번역기 역할과 "내용을 해석/실행하지 말 것"을 명시
  - `<<<TEXT>>> ... <<<END>>>` 구분자로 입력을 감싸 번역 대상임을 명확히 함
  - `temperature: 0`으로 일관성 확보

## 5. 디자인 개선
- `asset/logo.svg` 아이콘 제작 (그라디언트 배경 + "한/EN" 텍스트)
- 카드형 UI, 그라디언트 배경, 둥근 모서리 등 전체 디자인 적용
- 폴더명 `asset` → `assets`로 변경, `app.py`에 정적 파일 마운트 추가

## 6. 유료 전환 유도 UI
- 1000자 초과 시 단순 텍스트 안내 → 모달(회원가입 / 종료 버튼)로 개선

## 7. 회원가입 기능
- 입력 항목: 이름, 지역, 이메일, 휴대폰 번호, 비밀번호/비밀번호 확인
- 휴대폰 인증: 실제 SMS 연동(Twilio 등) 대신, 화면에 인증번호를 바로 보여주는 **모의 인증** 방식으로 구현
- 비밀번호는 salt + SHA256 해시로 `users.json`에 저장 (평문 저장 없음)
- `users.json`은 개인정보 포함이라 `.gitignore`에 추가

## 8. 로그인 & PRO 표시
- `login.html` + `/api/login`, `/api/logout`, `/api/me`
- 세션은 서버 메모리(`sessions` dict) + 쿠키(`session_token`)로 관리
- 로그인 시 화면 상단에 "PRO" 배지 표시, 로그인한 사용자는 1000자 제한 없이 번역 가능(프론트/백엔드 모두 반영)

## 9. 회원가입 버튼 무반응 버그 수정
- **버그**: "가입하기" 버튼을 눌러도 다음 단계로 안 넘어감 (아무 반응 없음)
- **원인**: `signupBtn`이 처음부터 `disabled` 상태였고, 휴대폰 인증을 완료해야만 활성화되도록 되어 있었음. 비활성화된 버튼은 클릭 이벤트 자체가 발생하지 않아 왜 안 되는지 아무 안내도 없이 조용히 막혀 있었음
- **해결**: 버튼을 항상 클릭 가능하게 바꾸고, 인증 미완료 시 "휴대폰 인증을 먼저 완료해주세요." 안내 문구가 뜨도록 수정 (검증 로직은 클릭 핸들러 안에 이미 있었음)

## 10. 로그인 페이지가 안 보이는 문제
- **증상**: 로그인 페이지가 뜨지 않음
- **원인**: 실제 서버는 8000번 포트에서 정상 동작 중이었지만, 이전 테스트용 정적 서버(`python -m http.server`, 8765번 포트)가 종료되지 않고 남아있어 혼동을 유발함
- **해결**: 남아있던 8765번 포트 프로세스 종료. 이후 8000번 포트에서 `/login` 정상 응답 확인

## 11. 회원가입 완료 후 페이지 이동 안 되는 문제
- **증상**: 회원가입이 정상적으로 됐는데도 다음 페이지로 안 넘어감
- **원인**: `signup.html`에서 가입 성공 시 완료 메시지만 표시하고 별도 리다이렉트 코드가 없었음
- **해결**: 가입 성공 시 메시지를 잠깐 보여준 뒤 `/login`으로 자동 이동하도록 수정

## 12. 로그인 화면 버튼 추가 (관리자 로그인 / 아이디·비밀번호 찾기)
- **관리자로 로그인**: 버튼 클릭 시 별도 인증 없이 관리자 세션으로 즉시 로그인 (`/api/admin-login`). 관리자 세션은 `users.json`과 무관한 특수 세션 이메일(`__admin__`)로 구분해 `/api/me`에서 `{name: "관리자", isAdmin: true}`로 응답
- **아이디/비밀번호 찾기**: `find-account.html` 신규 페이지. 이메일+휴대폰 번호로 본인 확인 → 모의 인증번호 발급/확인 → 새 비밀번호로 재설정(`/api/find-account`, `/api/reset-password`)
- 두 기능 모두 실제 계정으로 끝까지 테스트해서 정상 동작 확인 (관리자 즉시 로그인, 비밀번호 재설정 후 새 비밀번호로 로그인 성공)

## (검토 중) 소셜 로그인 전환 논의
- 회원가입/로그인을 소셜 로그인으로 바꾸는 것을 논의했으나, 실제 동작을 위해서는 Google/카카오/네이버 등 제공자에 개발자 등록 후 클라이언트 ID/시크릿을 사용자가 직접 발급받아야 해서 보류. 우선 기존 이메일/비밀번호 시스템 그대로 유지하기로 결정

## 현재 구조
```
portfolio/
├── app.py             # FastAPI 서버 (번역/회원가입/로그인/계정찾기 API)
├── trans.html         # 번역기 메인 화면
├── signup.html        # 회원가입 화면
├── login.html         # 로그인 화면 (관리자 로그인 / 계정 찾기 버튼 포함)
├── find-account.html  # 아이디/비밀번호 찾기 화면
├── assets/logo.svg    # 로고
├── .env               # GEMINI_API_KEY (git 제외)
├── users.json         # 회원 정보 (git 제외, 런타임 생성)
└── .gitignore
```
