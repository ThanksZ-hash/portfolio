# 한/영 번역기 기획서

## 1. 개요
- **프로젝트명**: 한/영 번역기 (trans.html)
- **목적**: Gemini API를 활용해 한국어 ↔ 영어 텍스트를 간단히 번역해주는 웹 프로그램
- **사용 환경**: 로컬 실행 (FastAPI + 정적 HTML)

## 2. 기술 스택
| 구분 | 내용 |
| --- | --- |
| 백엔드 | FastAPI |
| 프론트엔드 | 정적 HTML/CSS/JS (트랜스.html) |
| 번역 엔진 | Google Gemini API (`gemini-3.5-flash`) |
| 환경 설정 | `.env`에 `GEMINI_API_KEY` 저장 (서버에서만 사용, 클라이언트 노출 없음) |

## 3. 주요 기능
1. **텍스트 입력**: 사용자가 번역할 텍스트를 입력창에 작성
2. **번역 방향 선택**: 드롭다운(한국어 → English / English → 한국어)
3. **방향 전환 버튼**: 드롭다운 값을 원클릭으로 반대 방향으로 전환
4. **번역 실행**: "번역하기" 버튼 클릭 시 서버(`/api/translate`)에 요청 → Gemini API 호출 → 결과 표시
5. **글자 수 제한 및 유료 안내**: 입력 텍스트가 1000자를 초과하면 번역을 실행하지 않고 "무료 이용은 1000자까지 지원됩니다. 더 긴 텍스트를 번역하려면 유료 플랜으로 전환해주세요." 안내 문구 표시 (프론트/백엔드 이중 체크)

## 4. 화면 구성
- 제목: "한/영 번역기"
- 번역 방향 드롭다운 + 방향 전환 버튼
- 텍스트 입력창 (textarea)
- 번역하기 버튼
- 번역 결과 표시 영역

## 5. API 구조
| Method | Endpoint | 설명 |
| --- | --- | --- |
| GET | `/`, `/trans.html` | 번역기 화면(trans.html) 반환 |
| POST | `/api/translate` | `{ text, direction }` 요청 → `{ result }` 응답 |

## 6. 향후 개선 아이디어 (미정)
- 번역 이력 저장
- 다국어(한/영 외 언어) 확장
- 실제 유료 결제 연동
- 로딩/에러 UI 개선
