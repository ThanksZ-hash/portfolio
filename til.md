# 2026-07-29 TIL

## 오늘 진행한 것

### 1. Gemini API 학습
- Python SDK(`google-genai`) 기본 사용법 (`client.interactions.create`, `.env`로 API 키 관리)
- 브라우저에서 직접 API를 호출하면 API 키가 노출된다는 문제를 파악하고, 서버(FastAPI)가 대신 호출하는 구조로 전환

### 2. 웹 서비스 아키텍처 설계
- FastAPI + 정적 HTML의 가장 단순한 구조 학습: `/`는 HTML을 서빙, `/api/*`는 백엔드 로직 처리
- API라는 개념 학습: 프로그램끼리 정해진 형식(REST/JSON)으로 데이터를 주고받는 창구 — 오늘 Gemini 호출에 실제로 사용

### 3. 기능 구현 흐름
1. 한/영 번역기 기본 기능 (방향 선택, 전환 버튼)
2. 1000자 제한 + 유료 전환 안내 모달
3. 회원가입 (이름/지역/이메일/휴대폰/비밀번호) — 모의 휴대폰 인증, 비밀번호 salt+해시 저장
4. 로그인/로그아웃/세션(쿠키) + 로그인 시 "PRO" 배지, 글자 제한 우회
5. 관리자 원클릭 로그인, 아이디/비밀번호 찾기(본인확인 → 재설정)

### 4. 실전 디버깅 경험 (가장 값진 부분)
- **프롬프트 인젝션성 오작동**: 입력이 명령어처럼 보이면 Gemini가 번역 대신 마음대로 대답 → `system_instruction` + 구분자(`<<<TEXT>>>`)로 "데이터"와 "명령"을 분리해서 해결
- **UI 버그**: `disabled` 버튼은 클릭 이벤트가 아예 발생하지 않아 아무 피드백 없이 막힌다는 것을 발견 → 항상 클릭 가능하게 하고 안내 메시지로 대체
- **누락된 리다이렉트**: 성공 메시지만 있고 다음 페이지 이동 코드가 없어서 "안 넘어가는" 것처럼 보였던 케이스
- **서버 크래시 버그**: 외부 API(Gemini) 응답이 예상과 다르면 `KeyError`로 500 에러 — `try/except`로 방어적 코드 작성하는 법 학습

### 5. 보안/개인정보 감각
- API 키·개인정보 파일(`.env`, `users.json`)을 `.gitignore`로 관리
- 비밀번호는 평문 저장 대신 salt+SHA256 해시로 저장
- 제출용 zip에서 `.env` 제외 확인하는 습관

### 6. 마무리 산출물
- `기획서.md`, `개발과정.md`로 설계·트러블슈팅 과정을 문서화
- `프로젝트0729.zip`으로 과제 제출 패키징

## 배운 점
"기능 추가 → 실제로 눌러보며 재현 → 원인 추적 → 수정 → 재검증"을 반복하는 게 중요했다. 코드는 문법적으로 멀쩡해도 실행해봐야 드러나는 버그(disabled 버튼, KeyError 크래시 등)가 있다는 것을 직접 확인했다.
